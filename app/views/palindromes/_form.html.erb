<%= form_with(model: palindrome, local: true) do |form| %>
  <% if palindrome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(palindrome.errors.count, "error") %> prohibited this palindrome from being saved:</h2>

      <ul>
      <% palindrome.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phrase %>
    <%= form.text_field :phrase %>
  </div>

  <div class="field">
    <%= form.label :is_palindrome %>
    <%= form.check_box :is_palindrome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
